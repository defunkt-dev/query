<query-client-provider>
  <is-fetching/f />
  <is-mutating/m />
  <mutation-state/s options=() => ({})/>
  <div data-testid="fetching">${f}</div>
  <div data-testid="mutating">${m}</div>
  <div data-testid="states">${s.length}</div>
</query-client-provider>
